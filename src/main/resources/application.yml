spring:
  application:
    name: to-do

  datasource:
    url: jdbc:postgresql://localhost:5432/todo
    username: dev-user
    password: dev-password

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 5555
